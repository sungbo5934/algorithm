package com.algorithm.run.bfs;

import java.util.Scanner;

public class Robot {
	
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		String firstLine = sc.nextLine();
		String secondLine = sc.nextLine();
		
		int[][] arr = new int[Integer.parseInt(firstLine.split(" ")[0])][Integer.parseInt(firstLine.split(" ")[1])];
		Integer.parseInt(secondLine.split(" ")[0]);
		Integer.parseInt(secondLine.split(" ")[1]);
		Integer.parseInt(secondLine.split(" ")[2]);
	}

}
